# Copy to .env and set secure values before running
MYSQL_ROOT_PASSWORD=root_password_change_me
DB_NAME=video_store

# Database user for the application (change to a secure value)
DB_USER=video_user
DB_PASSWORD=video_pass_change_me
DB_HOST=127.0.0.1
DB_PORT=3306

# DMM API keys (do NOT share these publicly)
# Get your API credentials from: https://affiliate.dmm.com/
DMM_API_ID=your_api_id_here
DMM_AFFILIATE_ID=your_affiliate_id_here

# Admin token for the simple admin endpoint (choose a secure random string)
ADMIN_TOKEN=change_this_admin_token